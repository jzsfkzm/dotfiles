#!/bin/sh
#
# Quick shortcut to GitUp
#
# USAGE:
#
#   $ g
#   # => opens the current directory in GitUp
#
#   $ g .
#   $ g /usr/local
#   # => opens the specified directory in GitUp

if [ "$1" == "" ] ; then
  open . -a GitUp
else
  open $1 -a GitUp
fi
